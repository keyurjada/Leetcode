int findMin(int* nums, int numsSize) {
    printf("Enter size of an array : ");
    scanf("%d",&n);
    int* nums[n];
    for(int i=0;i<n;i++){
        scanf("%d",&numns[i]);
        if(nums[i]<nums[i+1]){
            nums[i] = nums[i+1];
        }
    }
}